# Julia Environment

You may attempt Julia environment management commands (e.g. `Pkg.add`, `Pkg.update`, `Pkg.rm`, `Pkg.resolve`, edits to `Project.toml`) once without asking. If you encounter errors or dependency conflicts, stop and ask the user to install all required packages, listing them explicitly.
